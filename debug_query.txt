user: Please design a plan to perform the following analysis -- display the plan but do not execute it yet:
We define non-micro stocks to be all stocks above the NYSE 20th percentile.
We sort stocks into price momentum characteristic terciles (top/middle/bottom third) with breakpoints based on non-micro stocks. 
For each tercile, we compute its "capped value weight", meaning that we weight stocks by their market equity
winsorized at the NYSE 80th percentile. 
The factor is then defined as the high-tercile return minus the low-tercile return.
Create a scatter plot of the factor against its benchmark returns.
Finally, evaluate the performance of the factor portfolio.

# Definitions
- NYSE: NYSE stocks have crsp_exchcd integer value equal 1.
- benchmark returns: The Panel ID for characteristics benchmark returns is the characteristic name followed by the string '_ret_vw_cap'. The characteristic name must be one that you created or was defined in the variables descriptions.
- factor: factor typically means the returns to a factor portfolio; factor returns are constructed from factor portfolio weights by calling suitable tool, while factor portfolio weights are constructed from raw scores or stock characteristics by calling other tools.


Manager Agent: I have designed a plan to perform the requested analysis. Here are the steps:

*   **Step 1**: Create a mask for NYSE stocks by filtering `crsp_exchcd` for a value of 1. This step will be handled by the `factor_agent_tool`.
*   **Step 2**: Define non-micro stocks by filtering `me` (market equity) to include only NYSE stocks and then identifying those stocks above the NYSE 20th percentile of market equity. This will result in a boolean mask. This step will be handled by the `factor_agent_tool`.
*   **Step 3**: Sort stocks into price momentum terciles (top/middle/bottom third) using the `ret_12_1` characteristic. The breakpoints for these terciles will be calculated exclusively from the non-micro stocks identified in the previous step. This step will be handled by the `factor_agent_tool`.
*   **Step 4**: Compute "capped value weight" by winsorizing the `me` (market equity) characteristic at the NYSE 80th percentile. This winsorized market equity will serve as the weighting scheme for portfolio construction. This step will be handled by the `factor_agent_tool`.
*   **Step 5**: Generate portfolio returns for each of the three terciles (high, middle, and low) using the capped value weights calculated in the previous step. This step will be handled by the `factor_agent_tool`.
*   **Step 6**: Define the factor returns by subtracting the returns of the low-tercile portfolio from the returns of the high-tercile portfolio. This step will be handled by the `factor_agent_tool`.
*   **Step 7**: Create a scatter plot of the calculated factor returns against its benchmark returns. The benchmark returns Panel ID will be `ret_12_1_ret_vw_cap`. This step will be handled by the `performance_agent_tool`.
*   **Step 8**: Evaluate the performance of the factor portfolio by computing and displaying relevant performance statistics. This step will be handled by the `performance_agent_tool`.

Please let me know when you are ready to execute this plan.
user: execute the plan
Factor Agent: I have completed all the steps assigned to me. Here is a summary of the operations performed and the resulting Panel IDs:

*   **Step 1**: Created a mask for NYSE stocks (`crsp_exchcd == 1`), resulting in Panel ID `_188`.
*   **Step 2**: Defined non-micro stocks by digitizing `me` based on the NYSE 20th percentile (`_189`) and then selecting stocks above this percentile, resulting in a boolean mask with Panel ID `_190`.
*   **Step 3**: Sorted stocks into price momentum terciles using `ret_12_1` with breakpoints based on non-micro stocks, resulting in Panel ID `_191`.
*   **Step 4**: Computed "capped value weight" by winsorizing `me` at the NYSE 80th percentile, using NYSE stocks as reference, resulting in Panel ID `_192`.
*   **Step 5**: Generated portfolio returns for the low momentum tercile (Tercile 1) and high momentum tercile (Tercile 3) using the capped value weights:
    *   Low momentum tercile returns: Panel ID `_196`.
    *   High momentum tercile returns: Panel ID `_200`.
*   **Step 6**: Defined the factor returns by subtracting the low-tercile returns from the high-tercile returns, resulting in Panel ID `_201`.

Now, I will provide the necessary Panel IDs for the `performance_agent_tool` to complete Steps 7 and 8.

**For Step 7 (Create a scatter plot):**
Factor returns Panel ID: `_201`
Benchmark returns Panel ID: `ret_12_1_ret_vw_cap`

**For Step 8 (Evaluate the performance):**
Factor portfolio returns Panel ID: `_201`

Performance Agent: The performance evaluation of the factor portfolio has been completed. Here are the results:

*   **Annualized Return**: 0.03837
*   **Max Drawdown**: -0.24537
*   **Sharpe Ratio**: 0.30486
*   **Volatility**: 0.12588
Performance Agent: The scatter plot of the factor returns against its benchmark returns has been created and saved to `file:///home/terence/Downloads/scratch/2024/JKP/media/plot__201.png`.

The performance evaluation of the factor portfolio is as follows:

*   **Annualized Return**: 0.03837
*   **Max Drawdown**: -0.24537
*   **Sharpe Ratio**: 0.30486
*   **Volatility**: 0.12588