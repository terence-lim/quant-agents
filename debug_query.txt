user: Please make a step-by-step plan to perform the following analysis -- display the plan but do not execute it:
We define non-micro stocks to be all stocks above the NYSE 20th percentile, where NYSE stocks have crsp_exchcd = 1.
We sort stocks into price momentum characteristic terciles (top/middle/bottom third) with breakpoints based on non-micro stocks. 
For each tercile, we compute its “capped value weight”, meaning that we weight stocks by their market equity
winsorized at the NYSE 80th percentile. 
The factor is then defined as the spread of long high-tercile stocks and short low-tercile stocks.
Factor portfolio returns are constructed as the dot product of price momentum spread portfolio weights and 1-month leading excess returns.
Create a scatter plot of price momentum factor portfolio returns shifted forward by one month against it benchmark returns,
where the PanelFrame ID for characteristics benchmark returns is the characteristic name followed by the string '_ret_vw_cap'. 
Finally, evaluate the performance of the factor portfolio.
Manager Agent: Here is a step-by-step plan to perform the analysis:

1.  **step number**: 1
    **description**: Filter for NYSE stocks by selecting observations where 'crsp_exchcd' is equal to 1.
    **agent tool**: factor_agent_tool
2.  **step number**: 2
    **description**: Determine the 20th percentile of 'market_equity' using only NYSE stocks (from step 1) as the indicator.
    **agent tool**: factor_agent_tool
3.  **step number**: 3
    **description**: Identify non-micro stocks by filtering 'market_equity' to include only those observations above the NYSE 20th percentile calculated in step 2.
    **agent tool**: factor_agent_tool
4.  **step number**: 4
    **description**: Winsorize 'market_equity' at the 80th percentile, using NYSE stocks (from step 1) as the indicator for percentile calculation. This will be used for capped value weighting.
    **agent tool**: factor_agent_tool
5.  **step number**: 5
    **description**: Sort stocks into terciles based on the 'ret_12_1' (price momentum) characteristic. The breakpoints for these terciles should be determined using only non-micro stocks (from step 3).
    **agent tool**: factor_agent_tool
6.  **step number**: 6
    **description**: Compute "capped value weights" for each tercile by weighting stocks within each tercile by the winsorized 'market_equity' obtained in step 4.
    **agent tool**: factor_agent_tool
7.  **step number**: 7
    **description**: Construct the price momentum factor as a spread portfolio by going long the high-tercile stocks and short the low-tercile stocks, using the capped value weights from step 6.
    **agent tool**: factor_agent_tool
8.  **step number**: 8
    **description**: Compute the factor portfolio returns by performing a matrix multiplication (dot product) of the price momentum spread portfolio weights (from step 7) and the 'ret_exc_lead1m' (1-month leading excess returns).
    **agent tool**: risk_agent_tool
9.  **step number**: 9
    **description**: Shift the factor portfolio returns (from step 8) forward by one month to align with the benchmark returns.
    **agent tool**: risk_agent_tool
10. **step number**: 10
    **description**: Create a scatter plot of the shifted factor portfolio returns (from step 9) against the benchmark returns, where the benchmark PanelFrame ID is 'ret_12_1_ret_vw_cap'.
    **agent tool**: risk_agent_tool
11. **step number**: 11
    **description**: Evaluate the performance of the factor portfolio returns (from step 8) by computing relevant performance statistics.
    **agent tool**: risk_agent_tool
user: Execute the plan.
Factor Agent: I have started executing the plan.

**Step 1: Filter for NYSE stocks.**
I used the `panelframe_isin` tool to filter the `crsp_exchcd` PanelFrame for observations where the exchange code is 1 (NYSE).
The resulting PanelFrame, representing NYSE stocks, has been cached with the ID `_29`.

**Step 2 & 3: Determine non-micro stocks.**
I attempted to compute 5 quantiles of `market_equity` using NYSE stocks (`_29`) as a reference to identify non-micro stocks (those above the 20th percentile). However, the `panelframe_quantiles` tool encountered an internal error: `ImportError: cannot import name 'quantiles' from 'qrafti'`.

Due to this tool error, I am unable to proceed with the analysis as I cannot determine the non-micro stocks, which are a prerequisite for subsequent steps. Therefore, I must stop the execution of the plan.
user: execute the plan
Factor Agent: Factor Agent: I have constructed the price momentum factor as a spread portfolio, going long the high-tercile stocks and short the low-tercile stocks, using the capped value weights. The PanelFrame representing these spread portfolio weights has been cached with the ID `_34`.

I have completed all steps assigned to the Factor Agent. The next steps (8-11) are assigned to the Risk Agent.
Factor Agent: I have completed the steps assigned to the Factor Agent.

**Step 1: Filter for NYSE stocks.**
I used the `panelframe_isin` tool to filter the `crsp_exchcd` PanelFrame for observations where the exchange code is 1 (NYSE). The resulting PanelFrame, representing NYSE stocks, has been cached with the ID `_35`.

**Step 2 & 3: Determine non-micro stocks.**
I used the `panelframe_quantiles` tool to compute 5 quantiles of `market_equity`, using the NYSE stocks (`_35`) as a reference. The resulting PanelFrame, with quantile assignments, has been cached with the ID `_36`.
Then, I used the `panelframe_isin` tool to identify non-micro stocks by filtering `_36` to include only those observations in quantiles 2, 3, 4, or 5 (i.e., above the 20th percentile). The PanelFrame representing non-micro stocks has been cached with the ID `_37`.

**Step 4: Winsorize 'market_equity' at the NYSE 80th percentile for capped value weighting.**
I used the `panelframe_winsorize` tool to winsorize `market_equity` at the 80th percentile, using NYSE stocks (`_35`) as the reference. The resulting PanelFrame, with winsorized market equity, has been cached with the ID `_38`.

**Step 5: Sort stocks into price momentum characteristic terciles with breakpoints based on non-micro stocks.**
I used the `panelframe_quantiles` tool to sort stocks into 3 terciles based on the `ret_12_1` (price momentum) characteristic. The breakpoints for these terciles were determined using only non-micro stocks (`_37`). The PanelFrame with tercile assignments has been cached with the ID `_39`.

**Step 6 & 7: Construct the price momentum factor as a spread portfolio using capped value weights.**
I used the `panelframe_spread_portfolios` tool to construct the price momentum factor. This involved going long the high-tercile stocks and short the low-tercile stocks (from `_39`), using the winsorized `market_equity` (`_38`) for capped value weighting. The PanelFrame representing these spread portfolio weights has been cached with the ID `_40`.

I have completed all steps assigned to the Factor Agent. The next steps (8-11) are assigned to the Risk Agent.
Risk Agent: Risk Agent: I encountered an error while trying to evaluate the performance of the factor portfolio. The `panelframe_performance_evaluation` tool failed with an `ImportError: cannot import name 'portfolio_summary' from 'qrafti'`. This prevents me from completing the performance evaluation.

I have completed all other steps assigned to the Risk Agent.
user: resume executing the plan
Risk Agent: I have resumed and completed the execution of the plan.

**Step 8: Compute the factor portfolio returns.**
I used the `panelframe_matmul` tool to compute the dot product of the price momentum spread portfolio weights (`_40`) and the 1-month leading excess returns (`ret_exc_lead1m`). The resulting factor portfolio returns have been cached with the ID `_43`.

**Step 9: Shift the factor portfolio returns forward by one month.**
I used the `panelframe_shift` tool to shift the factor portfolio returns (`_43`) forward by one month. The shifted returns have been cached with the ID `_44`.

**Step 10: Create a scatter plot of the shifted factor portfolio returns against the benchmark returns.**
I used the `panelframe_plot` tool to create a scatter plot of the shifted factor portfolio returns (`_44`) against the benchmark returns (`ret_12_1_ret_vw_cap`). The plot is titled "Price Momentum Factor Returns vs. Benchmark Returns" and saved as `file:///home/terence/Downloads/scratch/2024/JKP/media/plot__44.png`.

**Step 11: Evaluate the performance of the factor portfolio.**
I used the `panelframe_performance_evaluation` tool to evaluate the performance of the factor portfolio returns (`_43`) against the benchmark returns (`ret_12_1_ret_vw_cap`). The performance metrics are as follows:
- Annualized Return: 0.0397
- Max Drawdown: -0.2753
- Sharpe Ratio: 0.2895
- Volatility: 0.1373