user: Please make a step-by-step plan to perform the following analysis -- display the plan but do not execute it:
We define non-micro stocks to be all stocks above the NYSE 20th percentile.
We sort stocks into price momentum characteristic terciles (top/middle/bottom third) with breakpoints based on non-micro stocks. 
For each tercile, we compute its “capped value weight”, meaning that we weight stocks by their market equity
winsorized at the NYSE 80th percentile. 
The factor is then defined as the spread of high-tercile and low-tercile stocks.
Construct the factor returns and create a scatter plot against its benchmark returns.
Finally, evaluate the performance of the factor portfolio.

# Definitions
- NYSE: NYSE stocks have crsp_exchcd = 1.
- benchmark returns: The Panel ID for characteristics benchmark returns is the characteristic name followed by the string '_ret_vw_cap'. The characteristic name must be one that you created or was defined in the variables descriptions.

Manager Agent: Here is the step-by-step plan to perform the requested analysis:

1.  **step number**: 1
    **description**: Define non-micro stocks. First, filter for NYSE stocks (crsp_exchcd = 1). Then, identify non-micro stocks as those above the 20th percentile of market equity among NYSE stocks.
    **agent tool**: factor_agent_tool
2.  **step number**: 2
    **description**: Sort stocks into price momentum terciles. Sort stocks into terciles based on the 'ret_12_1' (price momentum t-12 to t-1) characteristic. The breakpoints for these terciles must be determined using only the non-micro stocks identified in Step 1.
    **agent tool**: factor_agent_tool
3.  **step number**: 3
    **description**: Compute capped value weights for each tercile. For each tercile, calculate the “capped value weight” for each stock. This involves winsorizing the 'market_equity' at the 80th percentile among all NYSE stocks and then using these winsorized market equity values as weights.
    **agent tool**: factor_agent_tool
4.  **step number**: 4
    **description**: Define the factor. Construct the factor as the spread between the high-tercile portfolio and the low-tercile portfolio.
    **agent tool**: factor_agent_tool
5.  **step number**: 5
    **description**: Construct factor returns. Generate the time series of returns for the factor defined in Step 4.
    **agent tool**: risk_agent_tool
6.  **step number**: 6
    **description**: Create a scatter plot of factor returns against benchmark returns. Generate a scatter plot comparing the factor returns (from Step 5) against its benchmark returns. The benchmark returns will be identified as 'ret_12_1_ret_vw_cap'.
    **agent tool**: risk_agent_tool
7.  **step number**: 7
    **description**: Evaluate the performance of the factor portfolio. Compute and display performance metrics for the factor portfolio.
    **agent tool**: risk_agent_tool
user: execute the plan
Factor Agent: I have executed the first four steps of the plan using the `default_api` tools.

Here's a breakdown of the steps performed:

**Step 1: Define non-micro stocks.**
1.  I first retrieved the panel IDs for `crsp_exchcd` and `market_equity` using `get_variables_descriptions`.
2.  I then created a panel (`_64`) to identify NYSE stocks by filtering `crsp_exchcd` for a value of 1 using `Panel_isin`.
3.  Next, I computed the market equity quintiles specifically for NYSE stocks. This was done using `Panel_quantiles` with `market_equity` as the input panel, `num=5` for quintiles, and `_64` (the NYSE stocks panel) as the `reference_panel_id`. This resulted in panel `_65`.
4.  Finally, I defined non-micro stocks as those above the 20th percentile of market equity among NYSE stocks. This was achieved by filtering the `_65` panel (market equity quintiles) for values 2, 3, 4, and 5 using `Panel_isin`, creating the `non_micro_stocks_panel` (`_66`).

**Step 2: Sort stocks into price momentum terciles.**
1.  I created a panel (`_67`) that sorts stocks into price momentum terciles. This was done using `Panel_quantiles` with `ret_12_1` (price momentum) as the input panel, `num=3` for terciles, and `_66` (the non-micro stocks panel) as the `reference_panel_id` to ensure breakpoints were based on non-micro stocks.

**Step 3: Compute capped value weights for each tercile.**
1.  I computed the "capped value weights" by winsorizing the `market_equity` at the 80th percentile among all NYSE stocks. This was done using `Panel_winsorize` with `market_equity` as the input panel, `upper=0.8`, and `_64` (the NYSE stocks panel) as the `reference_panel_id`. This resulted in the `capped_market_equity_nyse` panel (`_68`).

**Step 4: Define the factor.**
1.  I constructed the factor as the spread between the high-tercile and low-tercile portfolios. This was achieved using `Panel_spread_portfolios` with `_67` (the momentum terciles panel) as the `panel_id` and `_68` (the capped market equity weights panel) as the `weights_panel_id`. This created the `factor_panel` (`_69`).

The subsequent steps (constructing factor returns, creating a scatter plot, and evaluating performance) were assigned to the `risk_agent_tool` and were not executed by me.
Risk Agent: The performance of the factor portfolio has been evaluated, showing an annualized return of approximately 3.97%, a maximum drawdown of -27.53%, a Sharpe Ratio of 0.29, and a volatility of 13.73%.

All steps of the plan have been successfully executed.